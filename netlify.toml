[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Redirect /api/* to functions (already configured in function but explicit here too)
[[redirects]]
  from = "/api/sync"
  to = "/.netlify/functions/sync"
  status = 200
